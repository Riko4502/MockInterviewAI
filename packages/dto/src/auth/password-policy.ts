/** Минимальная длина пароля (Argon2id protection from resource exhaustion). */
export const PASSWORD_MIN_LENGTH = 12;

/** Максимальная длина пароля (защита от resource exhaustion при Argon2id). */
export const PASSWORD_MAX_LENGTH = 128;
